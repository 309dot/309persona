# 2025-01-11 개발 이력

## 작성자
AI Assistant (Frontend)

## 요약
사용자 이름 표시 로직 개선 - 5자 이상 시 말줄임표 처리

## 상세 변경점

### 1. 공통 유틸리티 함수 추가
- **파일**: `frontend/src/lib/formatName.ts` (신규)
- **기능**: 
  - 사용자 이름 포맷팅 유틸리티 함수 `formatVisitorName()` 추가
  - 5자 이상의 이름은 앞 5자만 표시하고 "..." 추가
  - "님" 호칭 자동 추가
  - 기본 이름 설정 지원 (기본값: "리크루터")

### 2. PersonaChatV2Page 수정
- **파일**: `frontend/src/pages/PersonaChatV2Page.tsx`
- **변경사항**:
  - `formatVisitorName` 유틸리티 함수 import
  - `withHonorific` 함수를 공통 유틸리티 사용하도록 리팩토링
  - 기본 이름을 "채용 담당자"로 유지

### 3. EntryPage 수정
- **파일**: `frontend/src/pages/EntryPage.tsx`
- **변경사항**:
  - `formatVisitorName` 유틸리티 함수 import
  - `displayName` 계산 로직을 공통 유틸리티 사용하도록 변경
  - JSX에서 중복 "님" 제거 (유틸리티 함수에서 처리)

### 4. ChatPage 수정
- **파일**: `frontend/src/pages/ChatPage.tsx`
- **변경사항**:
  - `formatVisitorName` 유틸리티 함수 import
  - `visitorDisplayName` 계산 로직을 공통 유틸리티 사용하도록 변경
  - JSX에서 중복 "님" 제거 (유틸리티 함수에서 처리)

## 사용자 경험 개선
- 긴 이름이 UI를 깨뜨리는 문제 방지
- 일관된 이름 표시 형식 (최대 5자 + "..." + "님")
- 모든 페이지에서 동일한 포맷팅 로직 적용

## 예시
- "홍길동" → "홍길동님"
- "채용담당자프리뷰" → "채용담당자...님"
- "김" → "김님"
- 빈 값 → "리크루터님" (또는 페이지별 기본값)

## 테스트 필요 사항
- [ ] 짧은 이름 (1-5자) 정상 표시 확인
- [ ] 긴 이름 (6자 이상) 말줄임표 처리 확인
- [ ] 빈 이름일 때 기본값 표시 확인
- [ ] 모든 페이지에서 일관된 표시 확인

## 관련 이슈
N/A

## 참고사항
- TypeScript 타입 체크 통과
- ESLint 린트 에러 없음
- 기존 기능에 영향 없음 (순수 표시 로직 변경)

---

## 루트 경로 리다이렉트 설정

### 작성자
AI Assistant (Frontend)

### 요약
루트 경로(/) 접속 시 자동으로 /persona로 리다이렉트

### 상세 변경점

#### App.tsx 라우팅 수정
- **파일**: `frontend/src/App.tsx`
- **변경사항**:
  - 루트 경로(`/`) → `/persona`로 자동 리다이렉트
  - EntryPage를 `/entry` 경로로 이동
  - 404 페이지(와일드카드)도 `/persona`로 리다이렉트

### 라우팅 구조
- `/` → `/persona` (자동 리다이렉트)
- `/entry` → EntryPage (기존 루트 페이지)
- `/chat` → ChatPage
- `/persona` → PersonaChatV2Page (메인 페이지)
- `/admin` → DashboardPage
- `/*` → `/persona` (404 처리)

### 영향
- 309designlab.com 접속 시 자동으로 309designlab.com/persona로 이동
- 사용자 경험 개선 (메인 페이지로 즉시 이동)

### 테스트 필요 사항
- [ ] 루트 도메인 접속 시 /persona로 리다이렉트 확인
- [ ] /entry 경로 접근 가능 확인
- [ ] 기존 /chat, /admin 경로 정상 작동 확인
